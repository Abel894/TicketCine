@model CrearPeliculaRequest
@{
    ViewData["Title"] = "Crear Película - TicketCine";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4>Crear Nueva Película</h4>
                </div>

                <div class="card-body">
                    <form method="post" enctype="multipart/form-data">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="Titulo" class="form-label">Título *</label>
                            <input asp-for="Titulo" class="form-control" placeholder="Ingresa el título de la película" required>
                            <span asp-validation-for="Titulo" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Sinopsis" class="form-label">Sinopsis *</label>
                            <textarea asp-for="Sinopsis" class="form-control" rows="4" placeholder="Ingresa la sinopsis" required></textarea>
                            <span asp-validation-for="Sinopsis" class="text-danger small"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="Genero" class="form-label">Género *</label>
                                    <input asp-for="Genero" class="form-control" placeholder="Ej: Acción, Drama" required>
                                    <span asp-validation-for="Genero" class="text-danger small"></span>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="DuracionMinutos" class="form-label">Duración (minutos) *</label>
                                    <input asp-for="DuracionMinutos" type="number" class="form-control" min="1" required>
                                    <span asp-validation-for="DuracionMinutos" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="Clasificacion" class="form-label">Clasificación *</label>
                                    <select asp-for="Clasificacion" class="form-select" required>
                                        <option value="">-- Selecciona una clasificación --</option>
                                        <option value="G">G (Público General)</option>
                                        <option value="PG">PG (Parental Guidance)</option>
                                        <option value="PG-13">PG-13</option>
                                        <option value="R">R (Restringida)</option>
                                        <option value="NC-17">NC-17</option>
                                    </select>
                                    <span asp-validation-for="Clasificacion" class="text-danger small"></span>
                                </div>
                            </div>

                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="Poster" class="form-label">Póster (Imagen)</label>
                                    <input asp-for="Poster" type="file" class="form-control" accept=".jpg,.jpeg,.png">
                                    <small class="form-text text-muted">Formatos: JPG, JPEG, PNG. Máx. 5 MB</small>
                                    <span asp-validation-for="Poster" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        @if (ViewData.ModelState.Values.Any(v => v.Errors.Count > 0 && v.Errors.All(e => e.ErrorMessage.Contains("Archivo"))))
                        {
                            <div class="alert alert-danger">
                                Debes seleccionar un archivo de imagen válido.
                            </div>
                        }

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                Crear Película
                            </button>
                            <a href="@Url.Action("Index")" class="btn btn-secondary">
                                Cancelar
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
